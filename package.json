{
  "name": "eyelert-monorepo",
  "private": true,
  "scripts": {
    "build": "npm --prefix mobile-app run build:core",
    "start": "npm run start:mobile",
    "start:mobile": "npm --prefix mobile-app run start",
    "start:dev:android": "npm --prefix mobile-app run start:dev:android",
    "android:dev": "npm --prefix mobile-app run android:dev",
    "build:android:dev": "npm --prefix mobile-app run build:android:dev"
  },
  "dependencies": {
    "ws": "^8.20.0"
  }
}
